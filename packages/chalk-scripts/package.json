{
  "name": "chalk-scripts",
  "license": "MIT",
  "version": "2.0.0",
  "author": "https://github.com/vercjames",
  "homepage": "https://github.com/vercjames/package-chalk-scripts",
  "description": "A stylized,Typescript based logging package for Node 😎" ,
  "keywords": [
    "logging",
    "chalk",
    "console",
    "colors"
  ],
  "main": "./build/index.js",
  "module": "./build/index.mjs",
  "types": "./build/index.d.ts",
  "files": [
    "build/**"
  ],
  "scripts": {
    "dev": "tsup src/index.ts --format cjs,esm --dts --out-dir build --watch",
    "build": "tsup src/index.ts --format cjs,esm --dts --out-dir build",
    "lint": "eslint src/**/*.ts",
    "format": "eslint src/**/*.ts --fix"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "@repo/config-typescript": "workspace:*",
    "tsup": "^8.0.2",
    "typescript": "^5.9.2",
    "eslint": "^9.33.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
